/** Set to `true` to show Shipment Collections in the sidebar and main area again. */
export const SHOW_SHIPMENT_COLLECTIONS = false;

/** Set to `true` to show Shipping Product Catalog in the sidebar and main area again. */
export const SHOW_SHIPPING_PRODUCT_CATALOG = false;
