
  # Shipments

  This is a code bundle for Shipments. The original project is available at https://www.figma.com/design/2MlneeDwRCGGvCukypW4aG/Shipments.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  